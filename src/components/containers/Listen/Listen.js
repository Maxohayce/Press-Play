import React from 'react';
import { Layout } from '../../presentational/Layout/index';

const Listen = () => (
    <Layout>
        <h1>Press play to listen</h1>
    </Layout>
);

export default Listen;