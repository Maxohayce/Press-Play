import React from 'react';

import Nav from '../Nav';

import './Footer.css';

const Footer = (props) => (
    <div className="Footer">
        <nav className="FooterNav">
            <Nav />
        </nav>
    </div>
)

export default Footer;